assembly-versioning-scheme: MajorMinorPatchTag
mode: Mainline
branches:
  master:
    increment: Patch
  dependabot:
    regex: dependabot
    source-branches:
    - develop
    - master
    is-release-branch: false
    track-merge-target: false
