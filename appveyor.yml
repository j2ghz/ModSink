version: 0.4.{build}
image: Visual Studio 2017

install:
  - choco install gitversion.portable -pre -y

before_build:
  - ps: gitversion /l console /output buildserver /updateAssemblyInfo
  - ps: msbuild /t:Restore /v:m

build:
  verbosity: minimal
artifacts:
- path: src\ModSink.WPF\bin\*\*\**\*