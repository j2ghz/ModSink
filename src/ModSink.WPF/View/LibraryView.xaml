<UserControl x:Class="ModSink.WPF.View.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ModSink.WPF.View"
             xmlns:common="clr-namespace:ModSink.Core;assembly=ModSink.Core"
             xmlns:vm="clr-namespace:ModSink.WPF.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500"
             d:DataContext="{d:DesignInstance vm:LibraryViewModel}">
    <DockPanel LastChildFill="True">
        <DockPanel LastChildFill="True" DockPanel.Dock="Left">
            <TextBox Text="Search" DockPanel.Dock="Top" />
            <ListBox ItemsSource="{Binding Modpacks}" SelectedItem="{Binding SelectedModpack}">
            </ListBox>
        </DockPanel>
        <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto" DataContext="{Binding SelectedModpack}">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="{Binding Name}" />
                <StackPanel Orientation="Horizontal">
                    <Button Content="Install" />
                    <Button Content="Play" />
                    <TextBlock Text="15GB" />
                </StackPanel>
                <TreeView ItemsSource="{Binding Mods}">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Mods}">
                            <TextBlock Text="{Binding Name}" />
                            <HierarchicalDataTemplate.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Mod}">
                                    <TextBlock Text="{Binding Hash}" />
                                </HierarchicalDataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                    <!--TreeViewItem Header="Mod1">
                        <TreeViewItem Header="✓ 18-8B-32-E1-F0-B2-E9-5C 15GB /addon/data/1.dat" />
                    < />
                    <TreeViewItem Header="Mod2">
                        <TreeViewItem Header="✓ B6-BB-1A-04-F8-8B-45-56 4GB /addon/data/3.dat" />
                        <TreeViewItem Header="✗ 02-FD-8D-3F-91-04-92-AA 1GB /addon/misc/5.wav" />
                    </TreeViewItem>
                                        <TreeView.ItemContainerStyle>
                        <Style TargetType="{x:Type TreeViewItem}">
                            <Setter Property="IsExpanded" Value="True" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeViewItem Header="Mod3" /-->
                </TreeView>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>